# 🚀 راهنمای آپدیت نسخه ۱۰.۳

## ✅ تغییرات انجام شده

### فایل‌های آپدیت شده:
1. **index.html** - عنوان به `Trainova v10.3` تغییر کرد
2. **_next/static/chunks/3c2aa57f730d0ddf.js** - تمام نسخه‌های `v۱۰` به `v10.3` تغییر کردند
3. **version.json** - نسخه به `10.3` ارتقا یافت
4. **sw.js** - استراتژی Network-First اضافه شد
5. **force-update.html** - صفحه جدید برای پاک‌سازی کش

## 📋 مراحل آپلود روی هاست

### مرحله ۱: پاک‌سازی کامل هاست
```bash
# تمام فایل‌های قدیمی رو پاک کنید
rm -rf index.html _next/ sw.js manifest.json version.json *.html
```

### مرحله ۲: آپلود فایل‌های جدید
فایل‌های زیر رو از پوشه `/workspace/trainova-static-ready (3)/trainova-static-ready/` آپلود کنید:

✅ **فایل‌های حیاتی:**
- `index.html` (نسخه 10.3)
- `_next/` (کل پوشه - شامل فایل‌های بیلد جدید)
- `sw.js` (Service Worker هوشمند)
- `version.json` (نسخه 10.3)
- `manifest.json`
- `force-update.html` (جدید)
- `loading.html` (جدید)
- `welcome.html` (جدید)
- `404-custom.html` (بهبود یافته)

### مرحله ۳: تست نهایی
1. سایت رو در حالت **ناشناس** (Incognito) باز کنید
2. Console رو چک کنید (`F12` → تب Console)
3. باید پیام `[Trainova kill] cleanup done` رو ببینید
4. نسخه `v10.3` رو در عنوان صفحه و فوتر بررسی کنید

## 🔍 چگونه مطمئن شویم آپدیت اعمال شده؟

### روش ۱: بررسی عنوان صفحه
- عنوان تب مرورگر باید `Trainova v10.3 — پلتفرم هوشمند تمرین و تناسب اندام | نسخه جدید با سیستم پاک‌سازی کش` باشه

### روش ۲: بررسی فوتر
- در پایین صفحه اصلی، باید `Trainova v10.3 · نسخه جدید با سیستم پاک‌سازی کش` رو ببینید

### روش ۳: Console
- `F12` رو بزنید
- به تب Console برید
- باید پیام‌های زیر رو ببینید:
  ```
  [Trainova kill] unregistering X SW(s)
  [Trainova kill] clearing X caches
  [Trainova kill] cleanup done
  ```

### روش ۴: بررسی نسخه JSON
- آدرس `https://trainova.ir/version.json` رو باز کنید
- باید `"version": "10.3"` رو ببینید

## ⚠️ اگر هنوز نسخه قدیمی نمایش داده می‌شه

### راه حل ۱: استفاده از force-update.html
به آدرس زیر برید:
```
https://trainova.ir/force-update.html
```
این صفحه تمام کش‌ها رو پاک می‌کنه و شما رو به نسخه جدید هدایت می‌کنه.

### راه حل ۲: پاک‌سازی دستی کش
1. `Ctrl + Shift + Delete` رو بزنید
2. "Cached images and files" رو انتخاب کنین
3. دکمه "Clear data" رو بزنین
4. صفحه رو Refresh کنین (`Ctrl + F5`)

### راه حل ۳: وارد کردن مستقیم clear.html
آدرس زیر رو مستقیماً در نوار آدرس وارد کنین:
```
https://trainova.ir/clear.html
```

## 🎯 نکات کلیدی

- ✅ **همه فایل‌ها رو یکجا آپلود کنید** - نه تکه تکه
- ✅ **اول فایل‌های قدیمی رو پاک کنید** - بعد فایل‌های جدید رو آپلود کنید
- ✅ **کش CDN رو هم پاک کنید** - اگه از CDN استفاده می‌کنید
- ✅ **تست در حالت ناشناس** - برای اطمینان از عدم وجود کش

## 📞 پشتیبانی

اگر مشکل ادامه داشت:
1. لاگ‌های Console رو بررسی کنید
2. نسخه `version.json` رو چک کنید
3. از صحت آپلود تمام فایل‌ها مطمئن شوید
